DELETE /api/admin/friend-link                         cors.New → OptionalAuth → Auth → RequirePermission → FriendLinkHandler.Delete
DELETE /api/admin/user/:id/content                    cors.New → OptionalAuth → Auth → RequirePermission → PurgeHandler.PurgeUserContent
DELETE /api/doc/article                               cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.DeleteArticle
DELETE /api/doc/category                              cors.New → OptionalAuth → Auth → RequirePermission → CategoryHandler.DeleteCategory
DELETE /api/doc/tag                                   cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.DeleteTag
DELETE /api/galgame-quiz/:id                          cors.New → OptionalAuth → Auth → QuizHandler.DeleteQuiz
DELETE /api/galgame-quiz/:id/comments/:postId         cors.New → OptionalAuth → Auth → ResourceCommentHandler.QuizDelete
DELETE /api/galgame-rating/:id                        cors.New → OptionalAuth → Auth → RatingHandler.DeleteRating
DELETE /api/galgame-rating/:id/comments/:postId       cors.New → OptionalAuth → Auth → ResourceCommentHandler.RatingDelete
DELETE /api/galgame-resource/:id/comments/:postId     cors.New → OptionalAuth → Auth → ResourceCommentHandler.ResourceDelete
DELETE /api/galgame/:gid                              cors.New → OptionalAuth → Auth → SubmissionHandler.Withdraw
DELETE /api/galgame/:gid/cover/:coverId/vote          cors.New → OptionalAuth → Auth → CoverVoteHandler.Unvote
DELETE /api/galgame/:gid/resource                     cors.New → OptionalAuth → Auth → ResourceHandler.DeleteResource
DELETE /api/galgame/collection/:cid                   cors.New → OptionalAuth → Auth → GalgameCollectionHandler.Delete
DELETE /api/galgame/comments/:postId                  cors.New → OptionalAuth → Auth → CommunityCommentHandler.Delete
DELETE /api/message/:id                               cors.New → OptionalAuth → Auth → MessageHandler.DeleteMessage
DELETE /api/toolset/:id                               cors.New → OptionalAuth → Auth → ToolsetHandler.Delete
DELETE /api/toolset/:id/comments/:postId              cors.New → OptionalAuth → Auth → ResourceCommentHandler.ToolsetDelete
DELETE /api/toolset/:id/resource                      cors.New → OptionalAuth → Auth → ResourceHandler.DeleteResource
DELETE /api/topic/:tid/comment                        cors.New → OptionalAuth → Auth → CommentHandler.DeleteComment
DELETE /api/topic/:tid/poll                           cors.New → OptionalAuth → Auth → PollHandler.DeletePoll
DELETE /api/topic/:tid/reply                          cors.New → OptionalAuth → Auth → ReplyHandler.DeleteReply
DELETE /api/topic/draft/:id                           cors.New → OptionalAuth → Auth → TopicDraftHandler.Delete
DELETE /api/update/history                            cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.DeleteHistory
DELETE /api/update/todo                               cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.DeleteTodo
DELETE /api/website-tag                               cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.DeleteWebsiteTag
DELETE /api/website/:domain                           cors.New → OptionalAuth → Auth → RequirePermission → WebsiteHandler.DeleteWebsite
DELETE /api/website/:domain/comments/:postId          cors.New → OptionalAuth → Auth → ResourceCommentHandler.WebsiteDelete
GET    /api/activity                                  cors.New → ActivityHandler.GetActivity
GET    /api/activity/tab                              cors.New → ActivityHandler.GetTab
GET    /api/activity/timeline                         cors.New → ActivityHandler.GetTimeline
GET    /api/admin/doc/article                         cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.GetAdminArticles
GET    /api/admin/galgame/submissions                 cors.New → OptionalAuth → Auth → RequireModerator → ClaimReviewHandler.PendingQueue
GET    /api/admin/overview/all                        cors.New → OptionalAuth → Auth → RequirePermission → OverviewHandler.GetOverview
GET    /api/admin/overview/stats                      cors.New → OptionalAuth → Auth → RequirePermission → OverviewHandler.GetStats
GET    /api/admin/permission-audit                    cors.New → OptionalAuth → Auth → RequireAdmin → PermissionAuditHandler.List
GET    /api/admin/role-permissions                    cors.New → OptionalAuth → Auth → RequireAdmin → RolePermissionHandler.GetMatrix
GET    /api/admin/trust/review-items                  cors.New → OptionalAuth → Auth → RequireModerator → TrustHandler.ListReviewItems
GET    /api/admin/trust/review-items/:id              cors.New → OptionalAuth → Auth → RequireModerator → TrustHandler.GetReviewItem
GET    /api/admin/user-permissions/:uid               cors.New → OptionalAuth → Auth → RequireAdmin → UserPermissionHandler.GetView
GET    /api/admin/user/:id/content-stats              cors.New → OptionalAuth → Auth → RequirePermission → PurgeHandler.GetUserContentStats
GET    /api/auth/me                                   cors.New → OptionalAuth → Auth → OAuthHandler.Me
GET    /api/category                                  cors.New → SectionHandler.GetCategories
GET    /api/doc/article                               cors.New → ArticleHandler.GetArticles
GET    /api/doc/article/:slug                         cors.New → ArticleHandler.GetArticleBySlug
GET    /api/doc/category                              cors.New → CategoryHandler.GetCategories
GET    /api/doc/tag                                   cors.New → TagHandler.GetTags
GET    /api/friend-link                               cors.New → FriendLinkHandler.List
GET    /api/galgame                                   cors.New → GalgameHandler.GetList
GET    /api/galgame-character/:id                     cors.New → EntityHandler.GetCharacterDetail
GET    /api/galgame-edit/mine                         cors.New → OptionalAuth → Auth → EditHandler.Mine
GET    /api/galgame-edit/proposals/:id                cors.New → OptionalAuth → Auth → EditHandler.ProposalDetail
GET    /api/galgame-edit/queue                        cors.New → OptionalAuth → Auth → RequireModerator → EditHandler.Queue
GET    /api/galgame-engine                            cors.New → EntityHandler.GetEngineList
GET    /api/galgame-engine/:id                        cors.New → EntityHandler.GetEngineDetail
GET    /api/galgame-official                          cors.New → EntityHandler.GetOfficialList
GET    /api/galgame-official/:id                      cors.New → EntityHandler.GetOfficialDetail
GET    /api/galgame-official/:id/relation-graph       cors.New → EntityHandler.GetOfficialRelationGraph
GET    /api/galgame-official/legacy/:id               cors.New → EntityHandler.ResolveLegacyOfficial
GET    /api/galgame-official/search                   cors.New → EntityHandler.SearchOfficials
GET    /api/galgame-quiz/:id                          cors.New → OptionalAuth → QuizHandler.GetQuizPlay
GET    /api/galgame-quiz/:id/answers                  cors.New → OptionalAuth → QuizHandler.GetQuizAnswers
GET    /api/galgame-quiz/:id/comments                 cors.New → OptionalAuth → ResourceCommentHandler.QuizList
GET    /api/galgame-quiz/:id/edit                     cors.New → OptionalAuth → Auth → QuizHandler.GetQuizForEdit
GET    /api/galgame-quiz/all                          cors.New → OptionalAuth → QuizHandler.GetAllQuizzes
GET    /api/galgame-quiz/mine/answered                cors.New → OptionalAuth → Auth → QuizHandler.GetMyAnswered
GET    /api/galgame-quiz/mine/favorites               cors.New → OptionalAuth → Auth → QuizHandler.GetMyFavorites
GET    /api/galgame-rating/:id                        cors.New → OptionalAuth → RatingHandler.GetRatingDetail
GET    /api/galgame-rating/:id/comments               cors.New → OptionalAuth → ResourceCommentHandler.RatingList
GET    /api/galgame-rating/all                        cors.New → RatingHandler.GetAllRatings
GET    /api/galgame-resource                          cors.New → OptionalAuth → ResourceHandler.GetResourceList
GET    /api/galgame-resource/:id                      cors.New → OptionalAuth → ResourceHandler.GetResourceDetail
GET    /api/galgame-resource/:id/comments             cors.New → OptionalAuth → ResourceCommentHandler.ResourceList
GET    /api/galgame-resource/:id/detail               cors.New → OptionalAuth → ResourceHandler.GetResourceDownloadDetail
GET    /api/galgame-series                            cors.New → EntityHandler.GetSeriesList
GET    /api/galgame-series/:id                        cors.New → EntityHandler.GetSeriesDetail
GET    /api/galgame-series/cards                      cors.New → EntityHandler.GetSeriesCards
GET    /api/galgame-staff/:id                         cors.New → EntityHandler.GetStaffDetail
GET    /api/galgame-tag                               cors.New → EntityHandler.GetTagList
GET    /api/galgame-tag/:id                           cors.New → EntityHandler.GetTagDetail
GET    /api/galgame-tag/multi                         cors.New → EntityHandler.GetMultiTagGalgames
GET    /api/galgame-tag/search                        cors.New → EntityHandler.SearchTags
GET    /api/galgame/:gid                              cors.New → OptionalAuth → GalgameHandler.GetDetail
GET    /api/galgame/:gid/collections/mine             cors.New → OptionalAuth → Auth → GalgameCollectionHandler.MyCollectionsForGalgame
GET    /api/galgame/:gid/comments                     cors.New → OptionalAuth → CommunityCommentHandler.List
GET    /api/galgame/:gid/comments/locate              cors.New → OptionalAuth → CommunityCommentHandler.Locate
GET    /api/galgame/:gid/edit/bootstrap               cors.New → OptionalAuth → Auth → EditHandler.Bootstrap
GET    /api/galgame/:gid/edit/diff                    cors.New → EditHandler.Diff
GET    /api/galgame/:gid/edit/proposals               cors.New → EditHandler.GameProposals
GET    /api/galgame/:gid/edit/revisions               cors.New → OptionalAuth → EditHandler.Revisions
GET    /api/galgame/:gid/link/all                     cors.New → OptionalAuth → GalgameProxyHandler.GetGalgameLinks
GET    /api/galgame/:gid/resource/all                 cors.New → OptionalAuth → ResourceHandler.GetGalgameResources
GET    /api/galgame/calendar                          cors.New → CalendarHandler.GetMonth
GET    /api/galgame/calendar/pending                  cors.New → CalendarHandler.GetPending
GET    /api/galgame/calendar/tba                      cors.New → CalendarHandler.GetTBA
GET    /api/galgame/calendar/upcoming                 cors.New → CalendarHandler.GetUpcoming
GET    /api/galgame/collection/:cid                   cors.New → OptionalAuth → GalgameCollectionHandler.GetDetail
GET    /api/galgame/drafts                            cors.New → DraftsHandler.GetDrafts
GET    /api/galgame/interactions/mine                 cors.New → OptionalAuth → Auth → GalgameHandler.MyInteractions
GET    /api/galgame/mine                              cors.New → Auth → SubmissionHandler.ListMine
GET    /api/galgame/myaudit                           cors.New → Auth → SubmissionHandler.ListAudit
GET    /api/galgame/search/picker                     cors.New → QuizHandler.SearchGalgames
GET    /api/galgame/search/wizard                     cors.New → Auth → SubmissionHandler.SearchWithPending
GET    /api/home                                      cors.New → HomeHandler.GetHome
GET    /api/message                                   cors.New → OptionalAuth → Auth → MessageHandler.GetMessages
GET    /api/message/admin                             cors.New → OptionalAuth → Auth → MessageHandler.GetSystemMessages
GET    /api/message/chat/history                      cors.New → OptionalAuth → Auth → ChatHandler.GetChatHistory
GET    /api/message/muted                             cors.New → OptionalAuth → Auth → MessageHandler.GetMutedMessages
GET    /api/message/nav/contact                       cors.New → OptionalAuth → Auth → ChatHandler.GetNavContact
GET    /api/message/nav/system                        cors.New → OptionalAuth → Auth → MessageHandler.GetNavSummary
GET    /api/perm/bundles                              cors.New → RolePermissionHandler.GetBundles
GET    /api/perm/mine                                 cors.New → OptionalAuth → Auth → UserPermissionHandler.GetMine
GET    /api/ranking/galgame                           cors.New → RankingHandler.GetGalgameRanking
GET    /api/ranking/topic                             cors.New → RankingHandler.GetTopicRanking
GET    /api/ranking/user                              cors.New → RankingHandler.GetUserRanking
GET    /api/report/reasons                            cors.New → OptionalAuth → Auth → TrustHandler.GetReasons
GET    /api/resource                                  cors.New → TopicHandler.GetResourceList
GET    /api/rss/galgame                               cors.New → RSSHandler.GetGalgameRSS
GET    /api/rss/topic                                 cors.New → RSSHandler.GetTopicRSS
GET    /api/search                                    cors.New → SearchHandler.Search
GET    /api/section                                   cors.New → SectionHandler.GetSectionTopics
GET    /api/toolset                                   cors.New → OptionalAuth → ToolsetHandler.GetList
GET    /api/toolset/:id                               cors.New → OptionalAuth → ToolsetHandler.GetDetail
GET    /api/toolset/:id/comments                      cors.New → OptionalAuth → ResourceCommentHandler.ToolsetList
GET    /api/toolset/:id/practicality                  cors.New → OptionalAuth → PracticalityHandler.GetPracticality
GET    /api/toolset/:id/resource/detail               cors.New → ResourceHandler.GetResourceDetail
GET    /api/topic                                     cors.New → OptionalAuth → TopicHandler.GetList
GET    /api/topic/:tid                                cors.New → OptionalAuth → TopicHandler.GetDetail
GET    /api/topic/:tid/poll/log                       cors.New → OptionalAuth → PollHandler.GetVoteLog
GET    /api/topic/:tid/poll/topic                     cors.New → OptionalAuth → PollHandler.GetPollsByTopic
GET    /api/topic/:tid/reaction/history               cors.New → OptionalAuth → TopicHandler.GetTopicReactionHistory
GET    /api/topic/:tid/reply                          cors.New → OptionalAuth → ReplyHandler.GetReplies
GET    /api/topic/:tid/reply/detail                   cors.New → OptionalAuth → ReplyHandler.GetReplyDetail
GET    /api/topic/:tid/reply/locate                   cors.New → OptionalAuth → ReplyHandler.GetReplyLocate
GET    /api/topic/:tid/reply/reaction/history         cors.New → OptionalAuth → ReplyHandler.GetReplyReactionHistory
GET    /api/topic/:tid/upvotes                        cors.New → OptionalAuth → TopicHandler.GetUpvotes
GET    /api/topic/draft                               cors.New → OptionalAuth → Auth → TopicDraftHandler.List
GET    /api/topic/draft/:id                           cors.New → OptionalAuth → Auth → TopicDraftHandler.Get
GET    /api/topic/interactions/mine                   cors.New → OptionalAuth → Auth → TopicHandler.MyInteractions
GET    /api/update/history                            cors.New → UpdateHandler.GetHistory
GET    /api/update/todo                               cors.New → UpdateHandler.GetTodos
GET    /api/user/:id                                  cors.New → UserHandler.GetProfile
GET    /api/user/:id/collections                      cors.New → OptionalAuth → GalgameCollectionHandler.GetUserCollections
GET    /api/user/:id/comments                         cors.New → UserHandler.GetUserComments
GET    /api/user/:id/floating                         cors.New → UserHandler.GetFloatingCard
GET    /api/user/:id/galgame-comments                 cors.New → UserHandler.GetUserGalgameComments
GET    /api/user/:id/galgames                         cors.New → UserHandler.GetUserGalgames
GET    /api/user/:id/ratings                          cors.New → UserHandler.GetUserRatings
GET    /api/user/:id/replies                          cors.New → UserHandler.GetUserReplies
GET    /api/user/:id/resources                        cors.New → UserHandler.GetUserResources
GET    /api/user/:id/toolsets                         cors.New → ToolsetHandler.GetUserToolsets
GET    /api/user/:id/topics                           cors.New → UserHandler.GetUserTopics
GET    /api/user/creator/status                       cors.New → Auth → CreatorHandler.Status
GET    /api/user/moemoepoint/log                      cors.New → Auth → UserHandler.GetMoemoepointLog
GET    /api/user/notification-preferences             cors.New → Auth → UserHandler.GetNotificationPreferences
GET    /api/user/search                               cors.New → Auth → UserHandler.SearchMention
GET    /api/user/status                               cors.New → Auth → UserHandler.GetStatus
GET    /api/website                                   cors.New → OptionalAuth → WebsiteHandler.GetWebsites
GET    /api/website-category/:name                    cors.New → CategoryHandler.GetWebsiteCategory
GET    /api/website-tag                               cors.New → TagHandler.GetWebsiteTags
GET    /api/website-tag/:name                         cors.New → TagHandler.GetWebsiteTagDetail
GET    /api/website/:domain                           cors.New → OptionalAuth → WebsiteHandler.GetWebsiteDetail
GET    /api/website/:domain/comments                  cors.New → OptionalAuth → ResourceCommentHandler.WebsiteList
GET    /healthz                                       cors.New → App.setupRoutes
PATCH  /api/galgame/collection/:cid                   cors.New → OptionalAuth → Auth → GalgameCollectionHandler.Update
POST   /api/admin/friend-link                         cors.New → OptionalAuth → Auth → RequirePermission → FriendLinkHandler.Create
POST   /api/admin/galgame/:gid/review                 cors.New → OptionalAuth → Auth → RequireModerator → ClaimReviewHandler.Review
POST   /api/admin/trust/review-items/:id/claim        cors.New → OptionalAuth → Auth → RequireModerator → TrustHandler.ClaimReviewItem
POST   /api/admin/trust/review-items/:id/decide       cors.New → OptionalAuth → Auth → RequireModerator → TrustHandler.DecideReviewItem
POST   /api/auth/logout                               cors.New → OAuthHandler.Logout
POST   /api/auth/oauth/callback                       cors.New → OAuthHandler.Callback
POST   /api/doc/article                               cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.CreateArticle
POST   /api/doc/category                              cors.New → OptionalAuth → Auth → RequirePermission → CategoryHandler.CreateCategory
POST   /api/doc/tag                                   cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.CreateTag
POST   /api/galgame-edit/proposals/:id/amend          cors.New → OptionalAuth → Auth → EditHandler.Amend
POST   /api/galgame-edit/proposals/:id/decline        cors.New → OptionalAuth → Auth → EditHandler.Decline
POST   /api/galgame-edit/proposals/:id/merge          cors.New → OptionalAuth → Auth → EditHandler.Merge
POST   /api/galgame-edit/proposals/:id/withdraw       cors.New → OptionalAuth → Auth → EditHandler.Withdraw
POST   /api/galgame-quiz                              cors.New → OptionalAuth → Auth → QuizHandler.CreateQuiz
POST   /api/galgame-quiz/:id/answer                   cors.New → OptionalAuth → Auth → QuizHandler.AnswerQuiz
POST   /api/galgame-quiz/:id/comments                 cors.New → OptionalAuth → Auth → ResourceCommentHandler.QuizCreate
POST   /api/galgame-rating                            cors.New → OptionalAuth → Auth → RatingHandler.CreateRating
POST   /api/galgame-rating/:id/comments               cors.New → OptionalAuth → Auth → ResourceCommentHandler.RatingCreate
POST   /api/galgame-resource/:id/comments             cors.New → OptionalAuth → Auth → ResourceCommentHandler.ResourceCreate
POST   /api/galgame/:gid/claim                        cors.New → OptionalAuth → Auth → SubmissionHandler.Claim
POST   /api/galgame/:gid/comments                     cors.New → OptionalAuth → Auth → CommunityCommentHandler.Create
POST   /api/galgame/:gid/edit/proposals               cors.New → OptionalAuth → Auth → EditHandler.Submit
POST   /api/galgame/:gid/edit/revert                  cors.New → OptionalAuth → Auth → EditHandler.Revert
POST   /api/galgame/:gid/resource                     cors.New → OptionalAuth → Auth → ResourceHandler.CreateResource
POST   /api/galgame/:gid/resubmit                     cors.New → OptionalAuth → Auth → SubmissionHandler.Resubmit
POST   /api/galgame/collection                        cors.New → OptionalAuth → Auth → GalgameCollectionHandler.Create
POST   /api/galgame/comments/:postId/flag             cors.New → OptionalAuth → Auth → CommunityCommentHandler.Flag
POST   /api/galgame/submit                            cors.New → OptionalAuth → Auth → SubmissionHandler.Submit
POST   /api/image/cover                               cors.New → OptionalAuth → Auth → ImageHandler.UploadCoverImage
POST   /api/image/galgame                             cors.New → OptionalAuth → Auth → ImageHandler.UploadGalgameImage
POST   /api/image/message                             cors.New → OptionalAuth → Auth → ImageHandler.UploadMessageImage
POST   /api/image/topic                               cors.New → OptionalAuth → Auth → ImageHandler.UploadTopicImage
POST   /api/message/chat/recall                       cors.New → OptionalAuth → Auth → ChatHandler.RecallChatMessage
POST   /api/message/chat/send                         cors.New → OptionalAuth → Auth → ChatHandler.SendChatMessage
POST   /api/report/submit                             cors.New → OptionalAuth → Auth → TrustHandler.SubmitReport
POST   /api/toolset                                   cors.New → OptionalAuth → Auth → ToolsetHandler.Create
POST   /api/toolset/:id/comments                      cors.New → OptionalAuth → Auth → ResourceCommentHandler.ToolsetCreate
POST   /api/toolset/:id/resource                      cors.New → OptionalAuth → Auth → ResourceHandler.CreateResource
POST   /api/toolset/:id/upload/abort                  cors.New → OptionalAuth → Auth → UploadHandler.UploadAbort
POST   /api/toolset/:id/upload/complete               cors.New → OptionalAuth → Auth → UploadHandler.UploadComplete
POST   /api/toolset/:id/upload/init                   cors.New → OptionalAuth → Auth → UploadHandler.UploadInit
POST   /api/toolset/:id/upload/resume                 cors.New → OptionalAuth → Auth → UploadHandler.UploadResume
POST   /api/topic                                     cors.New → OptionalAuth → Auth → TopicHandler.Create
POST   /api/topic/:tid/comment                        cors.New → OptionalAuth → Auth → CommentHandler.CreateComment
POST   /api/topic/:tid/poll                           cors.New → OptionalAuth → Auth → PollHandler.CreatePoll
POST   /api/topic/:tid/poll/vote                      cors.New → OptionalAuth → Auth → PollHandler.Vote
POST   /api/topic/:tid/reply                          cors.New → OptionalAuth → Auth → ReplyHandler.CreateReply
POST   /api/topic/draft                               cors.New → OptionalAuth → Auth → TopicDraftHandler.Save
POST   /api/trust/callback                            cors.New → TrustHandler.Callback
POST   /api/update/history                            cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.CreateHistory
POST   /api/update/todo                               cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.CreateTodo
POST   /api/user/avatar                               cors.New → Auth → ProfileHandler.UploadAvatar
POST   /api/user/check-in                             cors.New → Auth → UserHandler.CheckIn
POST   /api/user/creator/apply                        cors.New → Auth → CreatorHandler.Apply
POST   /api/website                                   cors.New → OptionalAuth → Auth → RequirePermission → WebsiteHandler.CreateWebsite
POST   /api/website-tag                               cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.CreateWebsiteTag
POST   /api/website/:domain/comments                  cors.New → OptionalAuth → Auth → ResourceCommentHandler.WebsiteCreate
PUT    /api/admin/friend-link                         cors.New → OptionalAuth → Auth → RequirePermission → FriendLinkHandler.Update
PUT    /api/admin/friend-link/reorder                 cors.New → OptionalAuth → Auth → RequirePermission → FriendLinkHandler.Reorder
PUT    /api/admin/galgame/:gid/resource-publish-ban   cors.New → OptionalAuth → Auth → RequirePermission → ResourceHandler.SetResourcePublishBan
PUT    /api/admin/role-permissions/:role              cors.New → OptionalAuth → Auth → RequireAdmin → RolePermissionHandler.Replace
PUT    /api/admin/user-permissions/:uid               cors.New → OptionalAuth → Auth → RequireAdmin → UserPermissionHandler.Replace
PUT    /api/doc/article                               cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.UpdateArticle
PUT    /api/doc/article/pin                           cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.SetArticlePin
PUT    /api/doc/article/reorder                       cors.New → OptionalAuth → Auth → RequirePermission → ArticleHandler.ReorderArticles
PUT    /api/doc/category                              cors.New → OptionalAuth → Auth → RequirePermission → CategoryHandler.UpdateCategory
PUT    /api/doc/tag                                   cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.UpdateTag
PUT    /api/galgame-quiz/:id                          cors.New → OptionalAuth → Auth → QuizHandler.UpdateQuiz
PUT    /api/galgame-quiz/:id/favorite                 cors.New → OptionalAuth → Auth → QuizHandler.ToggleQuizFavorite
PUT    /api/galgame-quiz/:id/quality                  cors.New → OptionalAuth → Auth → QuizHandler.RateQuizQuality
PUT    /api/galgame-rating/:id                        cors.New → OptionalAuth → Auth → RatingHandler.UpdateRating
PUT    /api/galgame-rating/:id/like                   cors.New → OptionalAuth → Auth → RatingHandler.ToggleLike
PUT    /api/galgame/:gid/collections                  cors.New → OptionalAuth → Auth → GalgameCollectionHandler.SetMembership
PUT    /api/galgame/:gid/cover/:coverId/vote          cors.New → OptionalAuth → Auth → CoverVoteHandler.Vote
PUT    /api/galgame/:gid/like                         cors.New → OptionalAuth → Auth → GalgameHandler.ToggleLike
PUT    /api/galgame/:gid/resource                     cors.New → OptionalAuth → Auth → ResourceHandler.UpdateResource
PUT    /api/galgame/:gid/resource/expired             cors.New → OptionalAuth → Auth → ResourceHandler.MarkExpired
PUT    /api/galgame/:gid/resource/like                cors.New → OptionalAuth → Auth → ResourceHandler.ToggleLike
PUT    /api/galgame/:gid/resource/valid               cors.New → OptionalAuth → Auth → ResourceHandler.MarkValid
PUT    /api/galgame/comments/:postId                  cors.New → OptionalAuth → Auth → CommunityCommentHandler.Update
PUT    /api/galgame/comments/:postId/like             cors.New → OptionalAuth → Auth → CommunityCommentHandler.ToggleLike
PUT    /api/message/admin/read                        cors.New → OptionalAuth → Auth → MessageHandler.MarkAdminRead
PUT    /api/message/system/read                       cors.New → OptionalAuth → Auth → MessageHandler.MarkAllRead
PUT    /api/toolset/:id                               cors.New → OptionalAuth → Auth → ToolsetHandler.Update
PUT    /api/toolset/:id/practicality                  cors.New → OptionalAuth → Auth → PracticalityHandler.UpsertPracticality
PUT    /api/toolset/:id/resource                      cors.New → OptionalAuth → Auth → ResourceHandler.UpdateResource
PUT    /api/topic/:tid                                cors.New → OptionalAuth → Auth → TopicHandler.Update
PUT    /api/topic/:tid/best-answer                    cors.New → OptionalAuth → Auth → TopicHandler.SetBestAnswer
PUT    /api/topic/:tid/comment                        cors.New → OptionalAuth → Auth → CommentHandler.UpdateComment
PUT    /api/topic/:tid/comment/like                   cors.New → OptionalAuth → Auth → CommentHandler.ToggleCommentLike
PUT    /api/topic/:tid/dislike                        cors.New → OptionalAuth → Auth → TopicHandler.ToggleDislike
PUT    /api/topic/:tid/favorite                       cors.New → OptionalAuth → Auth → TopicHandler.ToggleFavorite
PUT    /api/topic/:tid/hide                           cors.New → OptionalAuth → Auth → TopicHandler.ToggleHide
PUT    /api/topic/:tid/like                           cors.New → OptionalAuth → Auth → TopicHandler.ToggleLike
PUT    /api/topic/:tid/poll                           cors.New → OptionalAuth → Auth → PollHandler.UpdatePoll
PUT    /api/topic/:tid/reaction                       cors.New → OptionalAuth → Auth → TopicHandler.ToggleReaction
PUT    /api/topic/:tid/reply                          cors.New → OptionalAuth → Auth → ReplyHandler.UpdateReply
PUT    /api/topic/:tid/reply/dislike                  cors.New → OptionalAuth → Auth → ReplyHandler.ToggleReplyDislike
PUT    /api/topic/:tid/reply/like                     cors.New → OptionalAuth → Auth → ReplyHandler.ToggleReplyLike
PUT    /api/topic/:tid/reply/pin                      cors.New → OptionalAuth → Auth → ReplyHandler.PinReply
PUT    /api/topic/:tid/reply/reaction                 cors.New → OptionalAuth → Auth → ReplyHandler.ToggleReplyReaction
PUT    /api/topic/:tid/upvote                         cors.New → OptionalAuth → Auth → TopicHandler.Upvote
PUT    /api/update/history                            cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.UpdateHistory
PUT    /api/update/todo                               cors.New → OptionalAuth → Auth → RequirePermission → UpdateHandler.UpdateTodo
PUT    /api/user/bio                                  cors.New → Auth → ProfileHandler.UpdateBio
PUT    /api/user/notification-preferences             cors.New → Auth → UserHandler.UpdateNotificationPreferences
PUT    /api/user/username                             cors.New → Auth → ProfileHandler.UpdateUsername
PUT    /api/website-category                          cors.New → OptionalAuth → Auth → RequirePermission → CategoryHandler.UpdateWebsiteCategory
PUT    /api/website-tag                               cors.New → OptionalAuth → Auth → RequirePermission → TagHandler.UpdateWebsiteTag
PUT    /api/website/:domain                           cors.New → OptionalAuth → Auth → RequirePermission → WebsiteHandler.UpdateWebsite
PUT    /api/website/:domain/favorite                  cors.New → OptionalAuth → Auth → WebsiteHandler.ToggleFavorite
PUT    /api/website/:domain/like                      cors.New → OptionalAuth → Auth → WebsiteHandler.ToggleLike
